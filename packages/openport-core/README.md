# OpenPort Core Package

This package is the planned extraction target for the current protocol runtime in `src/`.

Planned contents:

- protocol types
- agent engine
- admin engine
- policy and audit abstractions
- adapter interfaces
- conformance-facing runtime entrypoints

Status:

- Created as a migration placeholder during Phase 0
- The live implementation still resides in the repository root `src/`
