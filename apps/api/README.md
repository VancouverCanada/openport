# OpenPort API App

This directory hosts the product API layer for OpenPort.

Current scope:

- NestJS application shell
- health endpoint
- `openport-core` runtime provider
- module boundaries for auth, workspaces, AI, and OpenPort admin

Status:

- Initialized during Phase 1
- Ready for auth/session/workspace/domain migration
