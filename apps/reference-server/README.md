# OpenPort Reference Server

This directory hosts the future standalone reference runtime for OpenPort.

Current scope:

- Fastify demo server shell
- local bootstrap integration creation
- agent and admin reference endpoints backed by `@openport/core`

Status:

- Initialized during Phase 3
- Root `src/app.ts` and `src/reference-server.ts` are still kept for compatibility
